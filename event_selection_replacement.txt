              <div style={{ display: 'block' }}>
                {EVENTS.map((event) => (
                  <div
                    key={event.id}
                    style={{
                      border: formData.event === event.id ? '2px solid #15803d' : '2px solid #ccc',
                      borderRadius: '8px',
                      padding: '16px',
                      marginBottom: '12px',
                      backgroundColor: formData.event === event.id ? '#f0fdf4' : '#ffffff',
                      cursor: 'pointer'
                    }}
                    onClick={() => setFormData(prev => ({...prev, event: event.id}))}
                  >
                    <input
                      type="radio"
                      name="event"
                      value={event.id}
                      checked={formData.event === event.id}
                      onChange={handleInputChange}
                      style={{ marginRight: '12px' }}
                    />
                    <span style={{ fontWeight: 'bold' }}>{event.name}</span>
                    <br />
                    <span style={{ color: '#666', fontSize: '14px' }}>
                      📅 {event.date} • 💰 ${event.price} per person
                    </span>
                  </div>
                ))}
              </div>
